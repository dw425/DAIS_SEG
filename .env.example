# =============================================================================
# DAIS SEG — Synthetic Environment Generator
# =============================================================================
# Copy this file to .env and fill in your values.

# --- Databricks Workspace ---------------------------------------------------
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
DATABRICKS_TOKEN=dapi...

# --- SEG Framework -----------------------------------------------------------
SEG_CATALOG=dais_seg
SEG_SCHEMA=blueprints
SEG_WAREHOUSE_ID=abc123def456
SEG_CLUSTER_ID=0123-456789-abcdef
SEG_NOTEBOOK_PATH=/Workspace/Repos/user/DAIS_SEG/notebooks

# --- Source System Credentials -----------------------------------------------
# These define the source database that SEG will profile and replicate.
# Upload this file via the Streamlit app, or set as environment variables.

SOURCE_TYPE=oracle
# Supported: oracle, sqlserver, postgresql, mysql, snowflake, teradata, db2

SOURCE_HOST=oracle.prod.example.com
SOURCE_PORT=1521
SOURCE_DATABASE=PROD
SOURCE_USER=migration_user
SOURCE_PASSWORD=your_password_here
SOURCE_CONNECTION_NAME=oracle_prod

# --- Snowflake-specific (only if SOURCE_TYPE=snowflake) ----------------------
# SOURCE_ACCOUNT=your_account
# SOURCE_WAREHOUSE=COMPUTE_WH
# SOURCE_ROLE=SYSADMIN

# --- Alternative: JDBC URL ---------------------------------------------------
# If you provide a JDBC URL, host/port/database/type are auto-detected.
# JDBC_URL=jdbc:oracle:thin:@oracle.prod.example.com:1521:PROD
# JDBC_URL=jdbc:sqlserver://sqlserver.prod.example.com:1433;database=PROD
# JDBC_URL=jdbc:postgresql://pg.prod.example.com:5432/prod_db
# JDBC_URL=jdbc:mysql://mysql.prod.example.com:3306/prod_db
